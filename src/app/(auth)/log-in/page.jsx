import LoginPage from "@/components/LogInPage";

export const metadata = {
    title: "Summer store | Log-In",
};

const LogIn = () => {
    return (
        <div>
            <LoginPage/>
        </div>
    );
};

export default LogIn;